// Client-safe permission keys (mirror of the server catalog; no server imports).
export { PERMISSIONS as PERMS } from "@/lib/auth/permissions";
export type { PermissionKey } from "@/lib/auth/permissions";
